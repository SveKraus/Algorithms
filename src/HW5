1. Идея заключается в разделении массива, опорным пунктом, берется элемент из центра. Те элементы которые будут меньше элемента
стоящего в центре, будут смещены в левую сторону, а большие в право. Разделяться массив будет до тех пор пока не останется один
элемент
2. В лучшем случае O(n×log n), в ср. случае O(n×log n), в худшем O(n^2)
3. не является устойчивой
4. метод выбора опорного элемента О(1)
  метод разбиения массива на две части от опорного элемента О(n)
  рекурсивный метод сортировки к каждым частям масива О(log n)

  5. написала код

7. в массиве выбираем опорный элемент (по середине) и разбиваем массив на две части где все элементы,
которые больше него встают после него в правую часть,
все остальные (которые меньше) в левую часть. Применяем рекурсивно алгоритм быстрой сортировки до тех пор пока не останется
1 элемент или 0 и после соединяем все в один массив
8.
public void reverseString(char[] s) {
        int left = 0;
        int right = s.length - 1;

        while (left < right) {

            char temp = s[left];
            s[left] = s[right];
            s[right] = temp;

            left++;
            right--;
        }
    }

9.public void zeroes(int[] num) {
          int left = 0;
            for (int i = 0; i < num.length; i++) {
              if (num[i] != 0) {

                  num[left] = num[i];
                  left++;
              }
          }
            for (int i = left; i < nums.length; i++) {
              num[i] = 0;
          }
      }
